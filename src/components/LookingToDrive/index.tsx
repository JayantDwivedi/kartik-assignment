import React from "react";
import styles from "./styles.module.scss";

const LookingToDrive = () => {
  return (
    <div className={styles.container}>
      <p className={styles.heading}>Looking to drive</p>
      <p className={styles.heading}>
        result? <span>Lets chat</span>
      </p>
    </div>
  );
};

export default LookingToDrive;
